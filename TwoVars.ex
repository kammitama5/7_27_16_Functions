#write a module that takes two values (c and d) and produces (2c + 3d).
#call it TwoVars and input= input1

defmodule TwoVars do
    def var(input, input1) do
        ((2*input) + (3*input1))
    end
end

